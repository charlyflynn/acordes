import { Text, VerticalStack } from "components";

const DesktopOnly = () => (
  <VerticalStack>
    <Text textAlign="center">this app currently only works on desktop</Text>
    <Text textAlign="center">apologies!</Text>
  </VerticalStack>
);

export default DesktopOnly;
